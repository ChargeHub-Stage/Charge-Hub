CREATE TABLE CarConnectNetworkCalls (
     id INTEGER PRIMARY KEY,
     lastTimeStamp TEXT NOT NULL
);

insertOrUpdateNetworkCall:
INSERT OR REPLACE INTO CarConnectNetworkCalls(id, lastTimeStamp)
VALUES (1, ?);

getLastNetworkCall:
SELECT lastTimeStamp FROM CarConnectNetworkCalls WHERE id = 1;